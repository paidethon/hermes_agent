# =============================================================================
# Zephyr AI Desktop — Nginx Basic Auth htpasswd Template
# =============================================================================
# 此文件为模板占位符，不包含真实密码。
# 运行时由 entrypoint.sh 的 generate_htpasswd() 函数从环境变量
# PORTAL_USER / PORTAL_PASSWORD 生成实际 .htpasswd 文件（bcrypt 加密）。
#
# 生成命令（entrypoint.sh 内执行）：
#   htpasswd -bcB /etc/nginx/.htpasswd "$PORTAL_USER" "$PORTAL_PASSWORD"
#   chmod 600 /etc/nginx/.htpasswd
#
# 手动生成（本地测试用）：
#   htpasswd -bcB .htpasswd admin 'your-strong-password'
#
# 注意：镜像层零密钥，真实密码只通过 ModelScope Secrets 注入。
# =============================================================================
